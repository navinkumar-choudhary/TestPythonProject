# OverLoading
def Add(n1, n2, n3=0):
        return(n1+n2+n3)

# Add(5,5) # function with Return keyword this won't work we need use the Call as Below
# Add(5,4,10)
print(Add(5, 6, 5))
print(Add(5, 10))
